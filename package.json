{
  "name": "nutriai-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix backend && npx prisma generate --schema=backend/prisma/schema.prisma",
    "start": "node backend/src/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
